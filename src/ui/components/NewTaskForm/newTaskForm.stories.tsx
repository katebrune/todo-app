import React from 'react';
import { NewTaskForm } from './newTaskForm';

export default { title: 'Components/NewTaskForm' };

// eslint-disable-next-line @typescript-eslint/no-empty-function
export const form = () => <NewTaskForm onSubmit={() => {}} />;
